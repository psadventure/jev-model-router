# Secrets — never commit
.env
.env.*
!.env.example

# Personal Claude Code settings and hook logs (may contain prompt snippets)
.claude/settings.local.json
*.log

# OS / tooling
.DS_Store
node_modules/
